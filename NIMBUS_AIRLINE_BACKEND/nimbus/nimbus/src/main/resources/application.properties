# ============================================
# FILE LOCATION: airline-backend/src/main/resources/application.properties
# UPDATED: Removed hardcoded credentials for security
# ============================================

# ===========================
# Server Configuration
# ===========================
server.port=8080

# ===========================
# Database Configuration
# IMPORTANT: Change these values before deployment!
# ===========================
spring.datasource.url=jdbc:mysql://localhost:3306/airline_db
spring.profiles.active=local
spring.datasource.username=root
spring.datasource.password=YOUR_PASSWORD_HERE
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===========================
# JPA/Hibernate Configuration
# ===========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.sql.init.mode=always

# ===========================
# Application Configuration
# ===========================
spring.application.name=Nimbus Airlines

# ===========================
# Error Handling
# ===========================
server.error.include-message=always
server.error.include-binding-errors=always

# ===========================
# Logging Configuration
# ===========================
logging.level.root=INFO
logging.level.com.airline.nimbus=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# ===========================
# File Upload Configuration (for future profile photos)
# ===========================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB